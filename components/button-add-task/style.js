import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    add: {
        alignItems: 'flex-end',
        shadowOffset: { height: 3 },
        shadowOpacity: 0.3,
        marginTop: -100,
        paddingBottom: 20,
        paddingRight: 15
    },
});
